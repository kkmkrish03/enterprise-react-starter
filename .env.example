# =========================================================================
# Bodhika Enterprise Platform - Environment Configuration Template
# =========================================================================

# Database connection defaults
# The platform runs entirely client-side using a browser LocalStorage database in development.
DB_CONNECTION=mock

# App settings
PORT=4200
NODE_ENV=development
VITE_AUTH_MODE=mock
